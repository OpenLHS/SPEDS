{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {
    "speds.ses.version": {
      "javaName": "spedsVersion",
      "description": "The version of the SPEDS protocol used by the Session layer.",
      "type": "string"
    },
    "speds.ses.reference": {
      "javaName": "spedsReference",
      "description": "The reference of the SPEDS protocol version used by the Session layer.",
      "type": "string"
    },
    "speds.ses.cert": {
      "javaName": "certificate",
      "description": "A valid certificate used to verify data integrity",
      "type": "string"
    },
    "speds.ses.private.key": {
      "javaName": "privateKey",
      "description": "The private key associated with the previous certificate used to sign digital data",
      "type": "string"
    }
  }
}
